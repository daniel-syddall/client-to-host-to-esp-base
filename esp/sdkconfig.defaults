# ESP-IDF SDK defaults for this project.
# These are applied on top of the chip defaults when running `idf.py set-target`.
# Override any value here to avoid modifying the generated sdkconfig.

# ── Console ───────────────────────────────────────────────────────────────── #
#
# UART0 is used for Pi communication (via the CH340 USB-UART bridge).
# Redirect the ESP-IDF console to the ESP32-S3 native USB (USB Serial/JTAG)
# so that log output does not corrupt the binary/JSON protocol stream.
#
# To view logs: connect the second USB port (native USB) to a host and run:
#   idf.py monitor -p /dev/ttyACM0   (or the assigned CDC port)
#
CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y

# ── FreeRTOS ──────────────────────────────────────────────────────────────── #

# 1 kHz tick rate (1 ms resolution).
CONFIG_FREERTOS_HZ=1000

# ── Logging ───────────────────────────────────────────────────────────────── #

# Default log level: INFO in production, DEBUG during development.
CONFIG_LOG_DEFAULT_LEVEL_INFO=y
# CONFIG_LOG_DEFAULT_LEVEL_DEBUG=y

# ── Flash size ────────────────────────────────────────────────────────────── #

CONFIG_ESPTOOLPY_FLASHSIZE_4MB=y

# ── ESP32-S3 specific ─────────────────────────────────────────────────────── #

# Use USB-Serial/JTAG controller for console on ESP32-S3 DevKit boards,
# or keep UART0 if using a separate USB-UART adapter.
# CONFIG_ESP_CONSOLE_USB_SERIAL_JTAG=y

# ── Watchdog ──────────────────────────────────────────────────────────────── #

# Disable the task watchdog on the idle task so long-running serial reads
# don't trigger a reset during development.
CONFIG_ESP_TASK_WDT_EN=n
